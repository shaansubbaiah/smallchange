import { SnakeToTitlePipe } from './snake-to-title.pipe';

describe('SnakeToTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new SnakeToTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
